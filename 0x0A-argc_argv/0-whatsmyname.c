#include <stdio.h>

/**
 * main - Takes two arguments
 * *@argc: An argument that returns the numerical value of argv
 * *@argv: A pointer to character string
 * Return: 0 when success
 */

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
