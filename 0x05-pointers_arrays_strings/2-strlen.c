#include "main.h"
#include <string.h>

/**
 * _strlen - output the length of a string
 * *@s: operand containing the string
 * Return: Always 0 (success)
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (0);
}
