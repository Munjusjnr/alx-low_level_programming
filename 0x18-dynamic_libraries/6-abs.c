#include "main.h"

/**
 * _abs - the function computes the absolute value of
 * an integer
 * * @n: So the paramater has to do with an integer
 * Return: -n if the n is less 0. n value if otherwise
 */

int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
