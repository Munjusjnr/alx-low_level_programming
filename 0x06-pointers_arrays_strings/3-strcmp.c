#include "main.h"
#include <string.h>

/**
 * _strcmp - comparison of two strings
 * *@s1: a pointer to first string
 * *@s2: a pointer to second string
 * Return: the results in numerical value
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);

}
